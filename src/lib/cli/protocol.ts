/**
 * Compatibility version for optional CLI upload features.
 *
 * Keep this separate from the web application's release version: only change
 * it when the CLI/server contract changes incompatibly.
 */
export const CLI_UPLOAD_PROTOCOL_VERSION = 1;
